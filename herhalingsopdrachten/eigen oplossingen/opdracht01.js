console.log("welkom in de wondere wereld van Web Expert 1");

let test; //declareren
let test = "test"; //initializeren

let list= [] //nieuwe array aanmaken

console.log(" de variablele =" + test + ". dit was een test");
Console.log(`de variabele = {test}.dit is een test`);

//string interpolation

function test() {
    var test ="nieuw"
}

// === altijd gebruiken!
 if (1=== '1') {

 }